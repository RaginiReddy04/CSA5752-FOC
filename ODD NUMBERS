#include<stdio.h>
#include<conio.h>
 int main(){
 	int i,n;
 	printf("\n enetr a number:");
 	scanf("%d",&n);
 	printf("\n odd numbers are between 1 and %d are:\n",n);
for(i=1;i<=n;i+=2)
{
	printf("%d",i);
	
}
getch();
}
